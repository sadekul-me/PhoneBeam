# PhoneBeam operator UI (M1)

Development:

```
npm install
npm run dev
```

Set `VITE_COORDINATOR_ORIGIN` if the coordinator is not `http://127.0.0.1:8080`.

This UI creates pairing sessions and displays QR / SAS. It does not receive remote media.
